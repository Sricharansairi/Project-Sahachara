// MITRA - Project Sahachara - Phase 1: Foundation Shell
// main.rs — Tauri application entry point

#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

use mitra_lib::run;

fn main() {
    run();
}
